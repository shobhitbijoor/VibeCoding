[project]
name = "backend"
version = "0.1.0"
description = "Kuzu Graph Database backend service for manufacturing data"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "kuzu>=0.9.0",
    "openpyxl>=3.1.0",
    "pandas>=2.2.0"
]
